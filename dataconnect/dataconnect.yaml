specVersion: "v1beta"
serviceId: "demo"
location: "asia-south1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "demo-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
